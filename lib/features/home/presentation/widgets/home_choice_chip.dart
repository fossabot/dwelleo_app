import '../../../../core/widgets/dwelleo_choice_chip.dart';

export '../../../../core/widgets/dwelleo_choice_chip.dart';

/// Thin alias kept after the chip moved to core (the properties filters
/// need it too). Existing home call sites stay unchanged.
typedef HomeChoiceChip = DwelleoChoiceChip;
