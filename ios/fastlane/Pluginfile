# No fastlane plugins are required for the iOS pipeline: TestFlight upload uses
# the built-in `upload_to_testflight` + `app_store_connect_api_key` actions.
# Add plugins here later with `bundle exec fastlane add_plugin <name>`.
